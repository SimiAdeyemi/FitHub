import 'package:flutter/material.dart';

class FoodTracker extends StatelessWidget {
  const FoodTracker({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text("Food Tracker Content"),
    );
  }
}
